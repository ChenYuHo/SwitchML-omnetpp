simple L2Queue
{
    //    parameters:
    //        int frameCapacity = default(0); // max number of packets; 0 means no limit
    gates:
        inout inside;
        inout outside;
        input directin;
}
